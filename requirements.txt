# Compatibility list mirroring the runtime dependencies in pyproject.toml.
# Use `uv sync` for development; pyproject.toml is the canonical dependency file.
vtk>=9.2
muscat>=2.5.2
meshio>=5.3
meshlane>=5.5.0
pandas
h5py
